/** @Public() 이 붙은 핸들러/컨트롤러를 표시하는 메타데이터 키(인증 생략). */
export const IS_PUBLIC_KEY = 'auth:isPublic';

/** @RequiredScopes(...) 로 요구 스코프를 표시하는 메타데이터 키. */
export const REQUIRED_SCOPES_KEY = 'auth:requiredScopes';

/** 모든 스코프를 허용하는 와일드카드. */
export const SCOPE_WILDCARD = '*';
